{
  "name": "media-search-agent",
  "version": "0.1.0",
  "description": "MediaSearchAgent desktop shell (Tauri + bundled-uv Python sidecar) - local-first semantic media search",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "license": "MIT",
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
